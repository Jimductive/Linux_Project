{% for backend in groups.backends -%}
      /home/wordpress-data/ {{ hostvars[backend].ansible_facts.default_ipv4.address }}/255.255.255.0(rw,no_root_squash,anonuid=1000,anongid=1000,sync)
{% endfor -%}