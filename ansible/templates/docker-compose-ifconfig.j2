version: "3.6"

services:
  ifconfig:
    image: ifconfig.io:latest
    build:
      context: ./
      target: production
    ports:
      - "{{ ansible_facts.ens4.ipv4.address }}:8080:8080"
    environment:
      TLS: ${TLS:-0}
      HOSTNAME: {{ ansible_facts.hostname }}
